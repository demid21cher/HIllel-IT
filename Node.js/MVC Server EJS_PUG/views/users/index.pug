doctype html
html(lang="uk")
  head
    meta(charset="UTF-8")
    title Список користувачів

  body
    h1 Список користувачів

    ul
      each user in users
        li
          a(href='/users/' + user.id)= user.name
          |  — #{user.email}
